# NDI Ingest-Service – Python Dependencies
# Versionsstrategie:
#   - Exakte Pins nur wo nötig (API-Stabilität)
#   - >= Pins für Pakete mit häufigen Abhängigkeitskonflikten

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Datei-Upload

# Datenbank
asyncpg==0.29.0                # async PostgreSQL
pgvector==0.2.5                # pgvector Python-Client
sqlalchemy[asyncio]==2.0.30    # ORM / Query-Builder

# Objektspeicher
minio==7.2.7

# Dokument-Parsing
tika==2.6.0                    # Apache Tika Python-Client
docling==2.5.0                 # Strukturerkennung (PDF, DOCX)

# Embeddings
sentence-transformers==3.0.1   # multilingual-e5-large
torch==2.3.0                   # CPU-Only reicht für Entwicklung

# NLP (M1-Vorbereitung; vollständige Nutzung ab M2)
# spaCy 3.8.x unterstützt typer>=0.12.5 – kompatibel mit docling
spacy>=3.8.4,<3.9.0

# Konfiguration & Utilities
pydantic==2.7.1
pydantic-settings>=2.3.0       # >=2.3.0 wegen docling
python-dotenv==1.0.1
httpx==0.27.0                  # async HTTP (Tika-Aufrufe)
tenacity==8.3.0                # Retry-Logik

# Logging & Observability
structlog==24.2.0
